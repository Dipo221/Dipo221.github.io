# 房間裡的貓 — 待辦

這支只放**還欠的**。做完就整條搬去 `CHANGELOG.md`，不要打勾留在原地——
打勾的清單只會越長越難掃，而且做完的事情該留下的是推理，不是一個核取方塊。

三份文件的分工：

| | |
|---|---|
| 根目錄 `README.md` | 怎麼玩、設計原則、怎麼跑測試、怎麼改美術 |
| `CHANGELOG.md` | 哪一版改了什麼、為什麼那樣改 |
| `TODO.md`（這支） | 還沒做的，跟為什麼還沒做 |

每一項寫四件事：**做什麼 / 為什麼想要 / 動到哪幾支 / 大概多大**。
「大概多大」只分小、中、大，不估時間——估了也不準，而且這不是在交差。

**編號有洞是正常的。** 做完的整條搬走，剩下的號碼不重排——
別的地方（CHANGELOG、程式註解）會用號碼指回這裡，重排會讓那些指標全部歪掉。
看到少了幾號，就是那幾號做完了，去 `CHANGELOG.md` 找。

---

## 先過這一關

新想法進來之前先問一句：

> **這個功能會不會讓「三天沒開」變成一件有罪惡感的事？**

會的話就不做，不管它多好玩。沒有飢餓值、沒有清潔度、沒有心情條、
沒有連續登入獎勵、沒有任何會因為你沒來而變差的數字。bond 只增不減，
`test.html` 有一條測試守著 `bondDelta >= 0`。

這條是地基不是規矩，完整的理由寫在 `world.js:8-13`：這個遊戲存在的意義
是忙完之後喘一口氣，不是再給一個 deadline。養成遊戲的本能反射會一直想加
這種東西回來，所以這段放在最前面，不是最後面。

**注意這條擋的是「因為你沒來所以變差」，不是「壞事」。**

Disi 今天沒抓到小動物很懊惱——那可以有，而且它根本不在這條規則的管轄範圍內。
明陽的說法是準的：**牠只對「沒抓到」這件事懊惱，懊惱一下就過了，跟你沒有關聯。
那就只是一條敘事詞。**

所以真正的判準有兩層，先問第一層：

1. **這東西裡面有沒有玩家？** 沒有的話它是敘事，不是狀態，直接放行。
   敘事詞的特徵是：沒有數值、不影響任何事、也修不好。牠的壞心情你插不了手，
   就像你也管不到牠昨天在想什麼。
2. **真的牽扯到玩家的東西，才問**：這件事會不會因為你來了就變好？
   會的話它就是在要求你出席，那就是衰減換了個樣子。

這兩層的實作差別很大：敘事詞是「日期 → 字串」的純函式，不進存檔、不被任何
邏輯讀取；狀態則要存、要讀、要有人維護。**能寫成敘事的就不要寫成狀態。**

---

## 現在的路線

兩條軌道，彼此不相依，可以交錯做。軌道 A 內部有先後順序，晚做會白做兩次。

**軌道 A 的主線 2026-09-05 收工了。** 第 2、3、15、18、5、14、17 項全部做完
搬去 `CHANGELOG.md` 了——房間重畫成閣樓、貓重畫成 24×24、兩個裝置都滿版、
按鈕列換成點房間裡的東西、**十件家具**、禮物箱。房間現在是滿的。

剩在這條軌道上的一項不是「還沒做完」，是**做完之後才長出來的**：

| | | 為什麼還在 |
|---|---|---|
| 6 | 窗外的天氣 | 那一層 2026-08-28 就拆好了，只欠往上加圖 |

（原本這裡還有第 13 項「看不見的橫向捲軸」，2026-09-11 明陽決定不做，
理由搬去「刻意不做」了——那一條同時記著哪天要重開它。
第 4 項「貓被家具遮住」同一天做完，搬去 `CHANGELOG.md` 了。）

**B · 氣氛**（只剩 9，跟 A 沒有相依）環境音。不碰美術。

**第 7＋11 項 2026-09-10 一起做完搬去 `CHANGELOG.md` 了**——桌機的提示改成
浮在畫面下方、6 秒淡掉，房間下面那截（只有主人看得到的計數、頁尾）整個
清空，計數搬進選單抽屜並多了一個累計數。

**第 8 項 2026-09-07 做完搬去 `CHANGELOG.md` 了**——Disi 現在會自己過一天：
摸牠會聽到今天過得怎樣，偶爾黃昏出門一下，回來會講去了多久。

**第 5 項提前做了，順序跟原本寫的相反。** 原本它排在 M3 後面，理由是
「M3 才會決定房間裡有哪些東西，現在接上去等家具進來又要再接一次」。
那個顧慮沒有成立：熱區是照 `room-data.js` 生的，**加家具不需要動這支程式**
——那份表本來就是為了這件事存在的。十件家具進來之後 `script.js` 只多了
「哪幾件要有熱區」那幾行，一格座標都沒有手抄。

**排家具那條限制留著給以後**：手機一次只看得到 6.1 欄（桌機 17.5），
所以「兩件家具擺在一起好不好看」在手機上幾乎問不到，
真正會被看到的是每一件單獨出現時的樣子。畫新東西進房間之前先看
`art/SIZES.md` 那 11 條規矩。

---

## A · 房間變成 8-bit（剩下的一項）

### 6. 窗外的天氣 — 中

**做什麼**：窗外除了跟著台北時間亮暗，也跟著**淡水真實的天氣**變。

**為什麼**：時間讓房間會呼吸，天氣讓它是**今天**。外面在下雨的時候
開著這一頁，窗外也在下雨——那一下就不是在看一張圖了。

**天氣要用淡水的，不是看的人所在地的。** 跟時區同一個道理：
Disi 住在淡水的房間，牠窗外下不下雨跟你在哪裡看沒有關係。
座標寫死 25.18, 121.44。

**API 選 Open-Meteo（`api.open-meteo.com`），因為它不用金鑰。**
這是硬條件不是偏好：這是純靜態站，任何金鑰都會直接寫在原始碼裡被看光。
中央氣象署的資料更權威，但要申請授權碼，公開 repo 放不了。
Open-Meteo 免費、無金鑰，回 WMO 天氣代碼，
對應成晴／陰／雨／雷雨／霧幾種就夠了。

**已經先探過真的端點**（2026-08-27），免得做到一半才發現不能用：

```
https://api.open-meteo.com/v1/forecast
  ?latitude=25.18&longitude=121.44
  &current=weather_code,is_day&timezone=Asia/Taipei
```

回 200、無金鑰、帶 `Origin` 時回 `Access-Control-Allow-Origin: *`，
所以純靜態站打得到。`current.weather_code` 是 WMO 代碼、`is_day` 是 0/1。
順帶一提牠回的座標會吸附到最近的網格點（25.13, 121.46），對淡水來說夠近。

**那一層 2026-08-28 已經拆好了**，這一項不用再動架構，只要往上加圖。

當初拆是為了修黃昏那扇灰窗（原因與經過見 `CHANGELOG.md`），但拆出來的東西
就是這一項要的：`.room-sky` 是四張整張房間大小、只有玻璃不透明的疊圖，
z-index 5，**疊在兩層時段層上面所以不吃 multiply**。天空色的單一事實來源是
`art/room.py` 的 `SKY_TOD`，`pixel.py` 照它算出 `art/room-sky-<時段>.png`。

**所以天氣要畫在哪裡**：再開一層跟 `.room-sky` **同構的兄弟層**
（`.room-weather`，z-index 6），一樣是整張房間大小、只有玻璃不透明。
雨絲、霧、雲照樣進 `room.py` 當資料算出來，不要手畫，也不要用 CSS 動畫
——房間裡的東西都是像素的，一條平滑的雨線會跟旁邊的硬邊打架。

分成兩層而不是把天氣揉進 `SKY_TOD`，是因為它們的維度不一樣：
時段有 4 種、天氣有 5 種，揉在一起要畫 20 張圖，分開只要 4 + 5 張。
晴天就是「沒有天氣層」，不用畫。

**動到哪**：`script.js` 加抓天氣那段、`style.css` 加 `.room-weather`
（照 `.room-sky` 那幾行複製，`?v=` 的規矩一樣）、`room.py` / `pixel.py` 加圖、
`world.js` 加「WMO 代碼 → 房間狀態」的純函式（那支不碰 DOM，所以測得到）。

（**注意**：`room.py` 的 `EMISSIVE` 存的是夜晚的顏色不是白天的。
窗已經有自己的一層了，但 `EMISSIVE` 還留著給兩盞燈用，
不要順手把它改成天空色——那會讓半夜的窗變成一片大白天。）

**兩條規矩，跟 Abacus 那次同一套**：

- **服務掛掉不能影響遊戲。** 抓不到天氣就退回只看時間的房間，貓照常跑。
  整頁離線也要能玩，這本來就是它的優點。
- **要快取。** 每次開頁都打一次 API 是浪費也不禮貌。
  存 localStorage 快取 30 分鐘左右——天氣本來就不會分鐘級地變。

---

## B · 氣氛

### 9. 背景音樂／環境音 — 中

**做什麼**：可以開的背景聲音，一顆按鈕開關。掛在副螢幕的時候有人陪。

**為什麼**：這是明陽對這個東西真正的用法——不是玩，是開著。
有聲音才真的像隔壁房間有一隻貓。

**版權是這一項的主要限制，跟當初挑素材包同一件事**
（`art/LICENSE.txt` 那整段比較）：**repo 是公開的，把音檔 commit 進來就是散布**，
所以授權必須明確允許散布，不能只是「可以用在你的專案裡」。

**建議的順序**：

1. **先做程式合成的環境音**——Web Audio 生白噪音／棕噪音，
   加一個低通濾波器就是雨聲或風聲的底。**零檔案、零授權、零下載量。**
   而且這跟這個 repo 的作風一致：貓是算出來的不是下載的，聲音也可以。
   **等第 6 項做完，聲音可以跟著窗外的天氣走**——外面下雨，聲音就是雨。
2. **melody 之後再說。** 真的要 lo-fi 的旋律就得找 CC0 或 CC BY 且明講可散布的
   曲子（Pixabay、Free Music Archive、Incompetech 都要一首一首確認），
   或者用 Web Audio 自己合成。先不擋，但不要為了它卡住整項。

**動到哪**：新的 `audio.js`（跟其他幾支一樣不碰 DOM，才測得到）、
`script.js` 接按鈕、`index.html` 加開關、`style.css`。

**四條規矩**：

1. **預設關，而且要記住選擇。** 存 localStorage（`cat-room:audio`，
   照 `<範圍>:<功能>` 慣例）。
2. **絕對不自動播放。** 瀏覽器本來就會擋，而且沒預期的聲音是很失禮的事。
   那顆按鈕正好就是 Web Audio 需要的那個使用者手勢。
3. **切到別的分頁要停或轉小。** 找不到聲音是從哪個分頁來的，很煩。
4. **音量要低，而且可調。** 這是背景，不是內容。

---

## 之前就想做、還沒排進路線的

### 10. 訪客貓 — 大

**做什麼**：別的貓偶爾路過窗外，或走進房間，Disi 有反應。

**為什麼**：第一輪選型時就講好「A 起手（一隻貓一個房間），之後加 B（訪客貓）」，
架構是照這個留的空間。目前完全沒有實作。

**動到哪**：幾乎全部。`cat.js` 的狀態機要能同時跑兩隻、`world.js` 要決定
誰什麼時候來（必須是時間的純函數，不然每個人看到的訪客都不一樣）、
`disi.py` 要畫第二隻貓、`script.js` 要多一個 sprite 元素。

**先想清楚再動**：訪客貓不能變成「你沒來就錯過了」。錯過感是衰減的變形，
一樣過不了最前面那一關。第 8 項的四條限制在這裡也適用。

### 21. 選單內容——禮物圖鑑 — 中

**2026-09-08 佔了位置**：選單列表裡加了「禮物圖鑑（敬請期待）」，
`disabled`，位置卡在音樂控制跟留言板中間（照原本規劃的順序）。
下面整條要做的事一件都還沒動。

**做什麼**：把 `world.js` 的 `GIFTS`（6 種）全部列出來，沒收集到的用
鎖住／灰階表示，收到的正常顯示原本的圖示。

**先過這一關**：這件事完全是既有 `state.gifts`（只增不減，`save.js`）的
另一種呈現，不新增任何狀態，不會因為沒開站而變差，通過。**但不能做成
進度條**——不寫「6 選 4」這種計數、不做百分比。紙箱當初的判斷是
「空箱子不是成就櫃」，圖鑑是同一條理由的延伸：讓人好奇還有什麼沒拿到，
不是讓人有「進度沒補完」的壓力。

**技術現況**：`GIFTS` 現在身兼兩職，既是「全部可能的禮物」也是拿來抽獎
的清單，跟「已收集」（`state.gifts`）從來沒有分開比對過。圖鑑要做的
其實只是「這個 id 在不在 `state.gifts` 裡」的判斷，不用動資料結構。

**鎖住的畫面先別急著畫新圖**：用 CSS `filter: grayscale() brightness()`
套在現有 `art/gifts.png` 上，零新檔案、零授權問題，做出來直接在瀏覽器
裡看效果。真的糊到看不出是「鎖住」而不是「壞掉」，再考慮另外畫一版
專門的鎖住圖（`art/gifts.py` 加一份）。

**建議跟紙箱合併，不要開兩套平行的禮物 UI**：點房間裡的箱子、或點選單的
「禮物圖鑑」，開的是同一個面板，只是面板現在永遠顯示全部 6 格而不是只
顯示已收集的。這是建議不是定案——做出來明陽看效果不喜歡再拆成兩個。

**動到哪**：`script.js` 的 `renderGifts()`、`style.css` 的 `.gift` 系列、
共用面板殼（`.panel-overlay`/`.panel-sheet`，第 16 項做的那顆，見
`script.js` 的 `showView()`）。

### 22. 選單內容——留言板 — 大（這項要新架一個後端）

**2026-09-08 佔了位置**：選單列表裡加了「留言板（敬請期待）」，
`disabled`，位置卡在禮物圖鑑跟更新日誌中間。下面那句「點進去先誠實
顯示『還在蓋』」還沒做——這次先用跟音樂控制、禮物圖鑑一樣的
`disabled` 佔位，沒有做成點得進去的子頁面。Worker 架好、真的要接資料
的時候再決定要不要照原計畫換成子頁面。

**做什麼**：訪客可以留言給 Disi，留言顯示在房間裡的 8bit 面板裡，
訪客彼此看得到對方留了什麼。

**存哪裡，2026-09-07 討論過**：選了自架 **Cloudflare Worker + KV/D1**。
考慮過的其他路都有各自過不去的理由——giscus 要訪客有 GitHub 帳號、
外觀是 iframe 套不進 8bit；Google 表單訪客會被導出房間，而且留言只有
我看得到、訪客彼此看不到；mailto 連結只是私訊，不是留言板。這是六項
選單內容裡唯一需要新基礎設施的一項，其餘五項在現有的純靜態架構裡都
做得完。

**架構**：Worker 開兩個 endpoint——POST 收新留言、GET 讀最近 N 則，
KV 或 D1 存資料。Worker 不一定要進這個 repo；真要留底可以放
`tools/cat-room/worker/` 當原始碼備份，實際部署用 `wrangler`，或另開
一個帶 Cloudflare API token 的 GitHub Actions——**token 存在 Actions
的 secret 裡，不進 repo**，跟這個站「不 commit 金鑰」的規矩是同一條
（`tools/open-now/` 的 GitHub token 也是存在瀏覽器 localStorage、
不進 repo 的同一個原則）。

**防灌水是硬要求**：一個公開、不設防的 POST 端點放著不會安靜——
遲早會被灌垃圾留言。上 Cloudflare Turnstile（免費、多數時候訪客
不用解任何謎題，跟 reCAPTCHA 那種要挑消防栓的不一樣）。

**審核先不做介面**：v1 直接在 Cloudflare dashboard 手動刪不該有的留言，
真的常常要用再考慮做一個管理頁面。

**先過這一關**：留言是訪客講給 Disi 聽的話，不影響 `bond`、不影響任何
遊戲機制，通過。留言板空的時候比照 `.gift-empty` 的態度處理——
不是「還沒有人留言喔」那種製造空虛感的提示句。

**建議跟音樂控制（第 20 項，已完成，見 `CHANGELOG.md`）同一個順序**：
先讓選單裡看得到「留言板」入口，點進去
先誠實顯示「還在蓋」，等 Worker 架好、資料真的串起來，再把殼換成
可以留言的樣子。這樣這一項慢慢弄的時候不會卡住其他 5 項。

**動到哪**：新的 Worker 程式（放哪裡執行時再定）、`script.js` 加
fetch 邏輯、共用面板殼、`style.css`。

---

## 刻意不做

這段的用處是**不要再分析第二次**。理由都推過了，寫在這裡是為了下次想到
同一個點子的時候，先看到「已經想過了，結論是不做」。

- **逐格精確同步**（所有人看到一模一樣的畫面）。要把時間切片、在切片內重播
  狀態機才做得到。而台北時區已經拿到九成效果——下午兩點 energy 是 0.12，
  進來的人幾乎一定看到一隻在睡的貓；黃昏進來一定看到牠在動。
  完整理由在 `world.js:173-176`。

- **關係層跨裝置同步**（筆電和手機共用一份 bond 和禮物）。要有後端存 `lastSeen`。
  Abacus 只能加、存不了時間戳；GitHub API 每次來訪都 commit 會洗爆版本紀錄，
  而且有 30 秒延遲。真的想要就得另外找後端，那是另一個決定。
  （2026-09-07 補記：這句「另一個決定」在留言板——第 22 項——那邊踩下去了，
  但範圍只到留言板，不含這裡講的跨裝置 bond／禮物同步，那條還是不做。）

- **把 private 層改成全站共用**。這個看起來像是「同步」的簡單版，其實會壞掉：
  `lastSeen` 一旦全域共用，只要站上有人來過就會被刷新，
  「你離開了五天」那一格永遠不會再觸發——路人會把我自己的離開時間用掉。

- **近大遠小**（貓走到後面變小）。像素圖縮放會糊掉（重畫成 24×24 之後也一樣，
  問題在非整數倍不在解析度），而且那是 3D 的解法，不是 8-bit 的解法。見第 4 項。

- **把光柱畫成像素的**（原第 12 項，2026-09-10 結案）。`.room-beam`（從窗戶斜下來
  那道光，`style.css` 的 `.room-beam`）是 CSS `clip-path` 多邊形，邊緣是平滑的斜線，
  整個房間、貓、家具全都是硬邊方格，只有它不是。這一項一直懸著不是因為難，
  是因為「該不該修」要明陽的眼睛判斷——**判斷結果是不修，現在這樣就很好看**。
  原本那條「平滑的光反而比較像光，8-bit 遊戲的階梯狀鋸齒是當年硬體逼出來的限制、
  不是好看」的猜測成立了。
  **要注意的是它不貴**：`room-light.png` 那層（`mix-blend-mode: screen` 的發光層，
  `pixel.py` 從磚號表產）早就在，光柱畫進那張圖就自動是像素的，還會跟著窗一起亮暗。
  所以下次看到那條平滑斜線，不會是「沒人做得動」——是不要做。不用再分析一次。

- **把藏起來的橫向捲軸還回來**（原第 13 項，2026-09-11 結案）。房間比視窗寬、
  左右可捲（`.room-bleed`），但捲軸被 `scrollbar-width: none` 和
  `::-webkit-scrollbar { display: none }` 藏掉了（`style.css` 的 `.room-bleed`），
  所以「這裡可以往旁邊捲」這件事沒有任何視覺提示。想過的三條路——做成像素風的
  自訂捲軸、邊緣加漸層或箭頭當「還有更多」的提示、乾脆把捲軸露出來——
  **結論是三條都不做，維持現狀**。
  **這一項有一半是無障礙，要誠實寫下來**：藏捲軸真正擋到的不是滑鼠使用者
  （他們可以拖曳、可以用滾輪），是**習慣拖捲軸的人**，以及看不到捲軸就不知道
  這一區可以捲的人。這是**知情之後接受的取捨，不是漏看**。
  接受的理由是既有的補償夠強：`script.js` 的 `follow()` 會跟著貓橫向捲，
  **房間自己會動**——不用捲也不會錯過貓，而「錯過貓」是這一區唯一會捲的理由。
  房間裡沒有藏在畫面外、非得手動捲過去才點得到的東西。
  哪天房間變成有東西只在畫面外（例如新家具擺到最邊邊），這個補償就失效了，
  那時候要重開這一項——**重開的觸發條件是那個，不是又有人覺得沒捲軸很怪**。

- **補上中英之間的空格**（2026-09-11 結案）。首頁工具卡的 `Disi住在這裡`
  （根目錄 `index.html`）和 `world.js` UPDATES 裡那幾句「Disi」後面沒空格，
  跟 repo 其他地方（`Disi 已經在這邊生活了 X 天`、CHANGELOG 的標題）不一致。
  **明陽看過，判斷是沒差，不改。** 排版潔癖很容易把這種東西撿起來當待辦，
  但它不影響任何人讀得懂什麼，撿一次就要分析一次，所以寫在這裡收掉。

- **近大遠小**（貓走到房間深處就縮小）。原本掛在第 4 項底下，那項 2026-09-11
  做完了，這個判斷要留著。**貓縮放會糊掉**——24×24 也一樣，非整數倍就是會糊；
  而且那是 3D 的解法不是 8-bit 的解法。縱深靠遮擋表現，不靠大小，
  而遮擋現在真的做出來了（`art/room.py` 的 `OCCLUDERS`）。

- **任何形式的衰減**。同最上面那一段。列在這裡是為了讓它出現在「不做」的
  清單上，看得見。

<!--
  這裡本來有一段「太小、不值得單獨開一版」，攢零碎的順手修。
  2026-09-11 清空了——裡面唯一那條（中英空格）明陽判斷不改，搬去「刻意不做」。
  下次真的又攢到零碎的東西再開回來，空著留一個標題只會讓人想找東西填。
-->

