# 尺寸表

要改一個小地方的時候，翻這張表指出**哪一張圖、哪一格**，比描述「窗戶左邊那條線」快得多。

數字有兩種單位，講的時候要分清楚是哪一種：

- **來源像素**：資料裡真正有幾格。改圖改的是這個。
- **螢幕像素**：瀏覽器放大之後的樣子。判斷「會不會太小」用這個。

放大倍率不是固定的，它由**可用高度 ÷ 列數**決定（見 `room.py` 的版面預算）。
在兩個基準裝置上算出來是：

| | 視窗 | 房間扣掉的高度 | 房間（螢幕） | 倍率 |
|---|---|---|---|---|
| 桌機 | 1440×900 | 434 | 847×466 | **2.65×** |
| 手機 | 375×812 | 340 | 859×472 | **2.68×** |

兩邊差不到 1%，所以下面「螢幕」那欄一律用桌機的 2.65×，記成 **1 來源像素 ≈ 2.65 螢幕像素**就夠用了。

---

## 房間

`art/room-pano.png` 與 `art/room-light.png`（兩張同尺寸，同一份資料算的）

| | 來源 | 螢幕 |
|---|---|---|
| 整間房間 | **320×176** | 847×466 |
| 一塊磚 | **16×16** | 42×42 |
| 格數 | 20 欄 × 11 列 | — |
| 一欄 | 16 | 42.4（= 房間寬的 5%） |
| 一列 | 16 | 42.4（= 房間高的 9.09%） |

### 列是怎麼分的

| 列 | 是什麼 | 來源 y |
|---|---|---|
| 0 | 屋脊 | 0–15 |
| 1 | 大樑（兩端收在斜屋頂裡） | 16–31 |
| 2–5 | 磚牆（兩端被斜屋簷吃掉，一列吃一格） | 32–95 |
| 6 | 踢腳板 | 96–111 |
| 7–10 | 木地板 | 112–175 |

貓的腳走得到第 **7.5–10.9** 列（來源 y 120–174）。上限不是 7.0，是因為貓站在第 7 列最上面時頭會頂進磚牆。

### 已經畫出來的物件

座標是**左上角對齊的那一格**，`room.py` 的 `place` 說了算，`room-data.js` 會跟著產。

| 名字 | 佔幾格 | 位置（欄, 列） | 來源 px 範圍 | 來源大小 | 螢幕 |
|---|---|---|---|---|---|
| `window` | 2×2 | (9, 2) | x 144–175, y 32–63 | 32×32 | 85×85 |
| `bowl` | 1×1 | (16, 9) | x 256–271, y 144–159 | 16×16 | 42×42 |

### 還沒畫的家具，位置已經先分好了

寫在 `room.py` 的房間註解裡，M3 照這個往下填：

| 欄 | 打算放什麼 |
|---|---|
| 1–4 | 床 |
| 5–8 | 書櫃、衣櫃 |
| 9–10 | 窗（已完成） |
| 11–14 | 書桌 |
| 15–18 | 地毯、碗（碗已完成） |

**最外面各兩欄（0–1、18–19）不能放高的東西**——斜屋簷在那裡把天花板壓到只剩幾列，
書櫃、衣櫃擺過去會直接穿出屋頂。這不是美感排序，是這間房間的形狀決定的。

---

## 貓

`art/disi-16.png`

| | 來源 | 螢幕 |
|---|---|---|
| 整張 sheet | **48×80** | — |
| 一格 | **16×16** | 42×42 |
| 排法 | 3 欄 × 5 排 | — |

一排一個姿勢：第 0 排 idle、1 walk（run 共用）、2 sleep、3 sit、4 eat。
`groom` 目前沒有自己的排，暫時指到 idle。

**這格數字會變。** M2 要把她重畫成 **24×24**（sheet 變成 72×144，3 欄 × 6 排，多一排 groom），
螢幕上就會回到 **64px**。現在的 42px 是暫時的——房間從 7 列變 11 列，同樣的高度要多分四份。

---

## 校對圖（跑 `python pixel.py` 才會生，不進版控）

要指某一格的時候看這幾張，它們都有放大倍率，所以格子數得出來：

| 檔案 | 尺寸 | 放大 | 看什麼 |
|---|---|---|---|
| `proof.png` | 1048×1732 | 20× + 格線 + 座標尺 | 貓的單格錯誤。**要講「(5,5) 那格」就看這張** |
| `squint.png` | 216×356 | 4× | 貓的剪影，縮小之後還認不認得出來 |
| `room-tiles.png` | 1332×356 | 9× + 每 4 格一條格線 | 每一塊磚攤開，房間版的 proof |
| `room-view.png` | 883×526 | 1×（就是桌機的真實大小） | 比例。綠框是重畫後的貓，橘框是手機一次看得到的範圍 |
| `room-tod.png` | 992×2274 | 3× | 晨／日／昏／夜四個時段並排 |
| `*.gif` | 128×128 | 8× | 動作，用遊戲裡真正的幀速 |

---

## 怎麼跟我講

講得出下面任一種我都接得住，由粗到細：

- **「碗往左移一格」** — 改 `room.py` 的 `place`，`room-data.js`、CSS、`script.js` 會自己跟上
- **「窗戶的下緣要再厚一格」** — 改 `OBJECTS["window"]` 底下那兩塊磚
- **「磚牆的灰漿太亮」** — 改 `PALETTE` 的 `m`，整面牆一起變
- **「proof.png 上 idle0 的 (5,5) 那格拿掉」** — 改 `disi.py` 的字元地圖

唯一要避開的講法是螢幕像素：「窗戶要大 10px」對不上任何一格，
因為倍率是 2.65 倍而且會隨視窗變。講**幾格**，那個永遠是對的。
